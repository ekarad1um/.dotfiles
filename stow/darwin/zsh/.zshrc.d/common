# code source files
alias -s c="$EDITOR"
alias -s cpp="$EDITOR"
alias -s h="$EDITOR"
alias -s hpp="$EDITOR"
alias -s py="$EDITOR"
alias -s rs="$EDITOR"
alias -s sh="$EDITOR"

# text files
alias -s log="$EDITOR"
alias -s md="$EDITOR"
alias -s txt="$EDITOR"

# configuration files
alias -s conf="$EDITOR"
alias -s list="$EDITOR"
alias -s toml="$EDITOR"
alias -s yaml="$EDITOR"
alias -s json="jless"

# media files
alias -s gif="open"
alias -s jpg="open"
alias -s jpeg="open"
alias -s png="open"
alias -s mp4="open"
alias -s mp3="open"
alias -s mov="open"
alias -s pdf="open"
